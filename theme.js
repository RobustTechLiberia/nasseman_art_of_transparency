// theme.js
import { createTheme } from "flowbite-react";

export const lightTheme = createTheme({
  button: {
    color: {
      primary: "bg-white text-red-900 hover:bg-white hover:text-red-900 focus:ring-0 border-none",
    },
  },
});
